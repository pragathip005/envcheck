# EnvTrust-Bench

This directory will hold EnvTrust-Bench: environment manifests, human-verified
ground-truth labels, and the leaderboard generation scripts.

Empty for now - it fills in once the `envcheck` tool itself works end-to-end
against the first few real environments (see the project plan, Phase 2-3).
